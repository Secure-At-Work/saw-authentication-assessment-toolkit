# Schemas

JSON Schema definitions for:

- `normalized/*.schema.json` — the Secure At Work normalized data shape each collector module must produce (one schema per collector, e.g. `authentication-methods.schema.json`).
- `rules.schema.json` — the shape of a rule definition consumed by the rules engine (`RuleID`, `Category`, `Setting`, `Expected`, `Severity`, `Recommendation`).

No schemas defined yet — this is scaffold only.
